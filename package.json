{
	"name": "kiruna",
	"version": "0.0.16",
	"description": "Docker orchestration and watchdog",
	"dependencies": {
		"async": "*",
		"dockerode": "2.x",
	    "express": "4.x",
		"keypress": "0.x",
	    "node-etcd": "~2.1.4",
		"request": "2.x",
		"semver": "3.x",
		"underscore": "1.x",
		"winston": "0.x",
		"winston-loggly": "1.x"
	},
	"devDependencies": {
		"grunt": ">=0.4.5",
		"grunt-dib": ">=0.1.2",
	    "grunt-exec": "*",
		"grunt-release": "1.x",
		"grunt-contrib-jshint": "*",
		"grunt-jasmine-node": "*"
	},
	"publishConfig": {
		"registry": "http://arvika-ssh:16073/"
	},
	"scripts": {
		"up": "docker run -it -v /var/run/docker.sock:/var/run/docker.sock -e IP=$IP kiruna"
	}
}